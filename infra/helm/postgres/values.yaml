replicaCount: 1

image:
  repository: postgres
  tag: "15"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5432

nodePool: postgres-pool

db:
  secretName: postgres-db-credentials
  userKey: username
  passwordKey: password
  databaseKey: database
