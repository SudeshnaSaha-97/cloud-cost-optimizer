apiVersion: v1
kind: Service
metadata:
  name: cloudsql-proxy
  namespace: default
spec:
  selector:
    app: cloudsql-proxy
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}
