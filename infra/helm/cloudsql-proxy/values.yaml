replicaCount: 1

image:
  repository: gcr.io/cloudsql-docker/gce-proxy
  tag: "1.33.3"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5432

nodePool: backend-pool   # optional, can be overridden

cloudsql:
  instance: ""   # injected via Helm --set cloudsql.instance
  credentialsSecret: cloudsql-db-credentials
