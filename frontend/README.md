# Frontend – React Dashboard

This module provides the **UI layer**:
- **Secure**: API calls routed through backend service.
- **Scalable**: Stateless React app served via Nginx in GKE.
- **Cost‑Optimized**: TailwindCSS for lightweight styling, minimal runtime overhead.

## Setup
```bash
cd frontend
npm install
npm start
