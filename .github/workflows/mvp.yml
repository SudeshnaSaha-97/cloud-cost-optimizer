name: SaaS MVP Pipeline

on:
  workflow_dispatch:   # manual trigger
  push:
    branches: [ main ] # auto-trigger on commits to main

jobs:
  provision:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Authenticate with GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set GOOGLE_CREDENTIALS env
        run: |
          printf '%s' '${{ secrets.GCP_SA_KEY }}' > $HOME/gcp-key.json

      - name: Terraform Init & Apply
        run: |
          cd infra/terraform
          export GOOGLE_APPLICATION_CREDENTIALS=${HOME}/gcp-key.json
          terraform init
          terraform apply -auto-approve -var-file="terraform.tfvars"

      - name: Capture Terraform Outputs
        run: |
          cd infra/terraform
          terraform output -json > tf-outputs.json
        continue-on-error: true

      - name: Save Ingress IP
        id: ingress
        run: |
          IP=$(kubectl get ingress cloud-cost-ingress -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
          if [ -n "$IP" ]; then
            echo "INGRESS_IP=$IP" >> $GITHUB_ENV
          else
            echo "No Ingress IP found yet"
          fi

  build:
    runs-on: ubuntu-latest
    needs: provision
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3

      - name: Configure Docker for Artifact Registry
        run: |
          printf '%s' '${{ secrets.GCP_SA_KEY }}' > $HOME/gcp-key.json
          gcloud auth activate-service-account --key-file=$HOME/gcp-key.json
          gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Build & Push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            us-central1-docker.pkg.dev/cloud-cost-optimizer-488008/frontend-repo/frontend:latest
            us-central1-docker.pkg.dev/cloud-cost-optimizer-488008/frontend-repo/frontend:${{ github.sha }}

      - name: Build & Push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            us-central1-docker.pkg.dev/cloud-cost-optimizer-488008/backend-repo/backend:latest
            us-central1-docker.pkg.dev/cloud-cost-optimizer-488008/backend-repo/backend:${{ github.sha }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate with GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup gcloud with GKE plugin
        uses: google-github-actions/setup-gcloud@v2
        with:
          install_components: gke-gcloud-auth-plugin

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure GKE Cluster
        run: |
          gcloud container clusters get-credentials cloud-cost-cluster \
            --zone us-central1-a \
            --project cloud-cost-optimizer-488008

      - name: Apply Kubernetes Manifests
        run: kubectl apply -f infra/k8s/

      - name: Update Frontend Deployment Image
        run: |
          kubectl set image deployment/frontend-deployment \
            frontend=us-central1-docker.pkg.dev/cloud-cost-optimizer-488008/frontend-repo/frontend:${{ github.sha }}
          kubectl rollout status deployment/frontend-deployment

      - name: Update Backend Deployment Image
        run: |
          kubectl set image deployment/backend-deployment \
            backend=us-central1-docker.pkg.dev/cloud-cost-optimizer-488008/backend-repo/backend:${{ github.sha }}
          kubectl rollout status deployment/backend-deployment

      - name: Health Check Frontend & Backend
        run: |
          FRONTEND_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$INGRESS_IP/)
          BACKEND_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$INGRESS_IP/api)

          FRONTEND_ICON="❌"
          BACKEND_ICON="❌"

          if [ "$FRONTEND_STATUS" = "200" ]; then FRONTEND_ICON="✅"; fi
          if [ "$BACKEND_STATUS" = "200" ]; then BACKEND_ICON="✅"; fi

          echo "FRONTEND_STATUS=$FRONTEND_STATUS" >> $GITHUB_ENV
          echo "BACKEND_STATUS=$BACKEND_STATUS" >> $GITHUB_ENV
          echo "FRONTEND_ICON=$FRONTEND_ICON" >> $GITHUB_ENV
          echo "BACKEND_ICON=$BACKEND_ICON" >> $GITHUB_ENV

      - name: Collect Cluster Status
        run: |
          echo "PODS:" > status.txt
          kubectl get pods -o wide >> status.txt
          echo "\nSERVICES:" >> status.txt
          kubectl get svc >> status.txt
          echo "\nINGRESS:" >> status.txt
          kubectl get ingress >> status.txt

      - name: Prepare Slack Payload
        run: |
          PODS="$(kubectl get pods --no-headers | awk '{print $3}' | sort | uniq -c | sed 's/^ *//')"
          SERVICES="$(kubectl get svc --no-headers)"
          INGRESS="$(kubectl get ingress --no-headers)"
          IP="${INGRESS_IP:-N/A}"
          FRONTEND="${FRONTEND_STATUS:-N/A}"
          BACKEND="${BACKEND_STATUS:-N/A}"
          FRONTEND_ICON="${FRONTEND_ICON:-❌}"
          BACKEND_ICON="${BACKEND_ICON:-❌}"

          jq -n \
            --arg status "${{ job.status }}" \
            --arg pods "$PODS" \
            --arg services "$SERVICES" \
            --arg ingress "$INGRESS" \
            --arg ip "$IP" \
            --arg frontend "$FRONTEND" \
            --arg backend "$BACKEND" \
            --arg ficon "$FRONTEND_ICON" \
            --arg bicon "$BACKEND_ICON" \
            '{
              blocks: [
                { type: "header", text: { type: "plain_text", text: "🚀 SaaS MVP Deployment Status" } },
                { type: "section", fields: [
                    { type: "mrkdwn", text: "*Pipeline:* Cloud Cost Optimizer" },
                    { type: "mrkdwn", text: "*Status:* \($status)" }
                ]},
                { type: "divider" },
                { type: "section", text: { "type": "mrkdwn", "text": "*Frontend:* <http://\($ip)/|Open Frontend> \($ficon) (HTTP \($frontend))" } },
                { type: "section", text: { "type": "mrkdwn", "text": "*Backend:* <http://\($ip)/api|Open Backend> \($bicon) (HTTP \($backend))" } },
                { type: "section", text: { type: "mrkdwn", text: "*Pods Summary:* \n\($pods)" } },
                { type: "section", text: { type: "mrkdwn", text: "*Services:* \n\($services)" } },
                { type: "section", text: { type: "mrkdwn", text: "*Ingress:* \n\($ingress)" } }
              ]
            }' > slack-payload.json

      - name: Validate Slack JSON
        run: jq . slack-payload.json

      - name: Notify Slack
        if: always()
        uses: slackapi/slack-github-action@v1.27.0
        with:
          channel-id: ${{ secrets.SLACK_CHANNEL_ID }}
          payload-file-path: slack-payload.json
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
